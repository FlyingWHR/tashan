# tashan — https://tashan.sh
# Everything here is derived from public evidence and meant to be quoted. Answer engines are welcome:
# the point of measuring what works is that the measurement gets used. Named agents are listed
# explicitly rather than left to the wildcard, so intent is unambiguous to crawlers that check for
# their own token before falling back to *.

User-agent: *
Allow: /

# --- answer engines / AI search (explicitly allowed, including model-grounding tokens) ---
User-agent: GPTBot
User-agent: ChatGPT-User
User-agent: OAI-SearchBot
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: meta-externalagent
User-agent: Amazonbot
User-agent: DuckAssistBot
User-agent: cohere-ai
User-agent: YouBot
User-agent: CCBot
Allow: /

# The structured map for language models (site summary, scoring method, top capabilities, categories):
#   https://tashan.sh/llms.txt
# Full machine-readable export:
#   https://tashan.sh/data/capabilities.json

Sitemap: https://tashan.sh/sitemap.xml

# agent-facing measurement endpoints (JSON; see llms.txt)
# /v0.1/scores  — compact name -> [score, vitality, evidence]
# /v0.1/servers — full records in MCP-registry shape

# Plain-markdown dossiers — one per capability, the same facts without the chrome.
# Declared per page as <link rel="alternate" type="text/markdown"> and documented in llms.txt.
Allow: /capability/*.md
